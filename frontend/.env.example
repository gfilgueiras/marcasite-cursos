VITE_API_URL=http://localhost:8080

# Opcional: só se você usar Stripe.js no browser (Payment Element, etc.).
# Deve ser a chave publishable do MESMO modo que o backend (test ou live).
# Sandbox: https://dashboard.stripe.com/test/apikeys → Chave publicável
# Live:    https://dashboard.stripe.com/apikeys (modo teste desligado)
VITE_STRIPE_PUBLISHABLE_KEY=
